<div class="card text-center">
    <div class="card-header">
        <h3>{{curUser.name}}</h3>
    </div>
    <div class="card-body">
        <h5 class="card-title">{{user.type}}</h5>
        <p class="card-text">description: {{user.description}} </p>
    </div>
    <div class="card-footer text-muted">
        <a type="button" class="btn btn-light" href="/user/{{user._id}}/changepassword">Change Password</a>
    </div>
</div>